[package]
name = "detour-lib"
version = "0.1.0"
edition = "2021"
autotests = false

[[test]]
name = "tests"
path = "tests/progress.rs"

[dev-dependencies]
trybuild = { version = "1.0", features = ["diff"] }

[dependencies]
detour-lib-impl = { path = "impl" }

